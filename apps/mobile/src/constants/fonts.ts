import {
  Inter_400Regular,
  Inter_500Medium,
  Inter_600SemiBold,
  Inter_700Bold,
  Inter_800ExtraBold,
} from '@expo-google-fonts/inter';

/**
 * The five Inter weights the type scale uses. Keys must match the
 * `fontFamily` values in theme.ts, because React Native resolves a custom
 * family by name and cannot synthesise the others from one file.
 */
export const interFonts = {
  Inter_400Regular,
  Inter_500Medium,
  Inter_600SemiBold,
  Inter_700Bold,
  Inter_800ExtraBold,
};
